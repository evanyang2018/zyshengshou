<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="group.yf.dao.GradeSegmentMapper">
	
	<select id="selectRanking" resultType="Integer">
		SELECT ranking FROM  province p, scoresegment s
		WHERE p.name=#{province} AND p.id=s.provinceId AND s.score=#{score}
	</select>
	
	

</mapper>